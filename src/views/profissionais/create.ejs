<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Novo Profissional</title>
    <style>
        body { font-family: sans-serif; padding: 20px; max-width: 600px; margin: auto; }
        
        form { 
            display: flex; 
            flex-direction: column; 
            gap: 15px; 
            border: 1px solid #ddd; 
            padding: 20px; 
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1); /* Sombra suave */
        }
        
        label { font-weight: bold; margin-bottom: 5px; }
        input { padding: 10px; border: 1px solid #ccc; border-radius: 4px; }
        
        /* Botão Salvar (Verde para combinar com a listagem) */
        button { 
            padding: 10px; 
            background: #28a745; 
            color: white; 
            border: none; 
            cursor: pointer; 
            font-size: 16px; 
            border-radius: 4px; 
            margin-top: 10px;
        }
        button:hover { background: #218838; }
        
        .titulo { text-align: center; color: #333; }
        
        /* Estilo do link Voltar */
        .btn-cancel { 
            display: block; 
            text-align: center; 
            margin-top: 15px; 
            color: #6c757d; 
            text-decoration: none; 
            font-size: 0.9em;
        }
        .btn-cancel:hover { text-decoration: underline; color: #333; }
    </style>
</head>
<body>

    <h2 class="titulo">Cadastrar Novo Profissional</h2>

    <form action="/profissionais" method="POST">
        <label for="nome">Nome do Profissional:</label>
        <input type="text" name="nome" id="nome" placeholder="Ex: João Barbeiro" required>

        <label for="especialidade">Especialidade:</label>
        <input type="text" name="especialidade" id="especialidade" placeholder="Ex: Corte, Barba, Platinado" required>

        <button type="submit">Salvar Cadastro</button>
    </form>
    
    <a href="/profissionais" class="btn-cancel">Cancelar e Voltar</a>

</body>
</html>