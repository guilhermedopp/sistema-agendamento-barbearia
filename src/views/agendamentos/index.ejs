<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Agenda</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; padding: 20px; background-color: #fdfdfd; }
        
        /* --- Cabeçalho --- */
        .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 25px; padding-bottom: 15px; border-bottom: 2px solid #eee; }
        .header h1 { margin: 0; color: #333; font-size: 1.8em; }
        .user-info { display: flex; align-items: center; gap: 15px; font-weight: 500; }
        .badge-admin { background-color: #343a40; color: #fff; padding: 4px 8px; border-radius: 4px; font-size: 0.75em; font-weight: bold; letter-spacing: 0.5px; }
        
        /* Botão de Logout estilizado */
        .btn-logout {
            color: #dc3545;
            text-decoration: none;
            padding: 6px 12px;
            border: 1px solid #dc3545;
            border-radius: 4px;
            font-size: 0.9em;
            transition: all 0.2s ease-in-out;
        }
        .btn-logout:hover { background-color: #dc3545; color: white; }


        /* --- Navegação Principal (Botões Outline) --- */
        .nav-links { margin-bottom: 25px; padding: 15px; background-color: #fff; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.05); border: 1px solid #eee; display: flex; flex-wrap: wrap; gap: 10px; }
        .nav-btn {
            display: inline-block;
            text-decoration: none;
            color: #555;
            font-weight: 600;
            padding: 8px 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            background-color: #fff;
            transition: all 0.2s;
            font-size: 0.95em;
        }
        .nav-btn:hover { background-color: #e9ecef; color: #333; border-color: #adb5bd; }


        /* --- Painel Admin --- */
        .admin-panel { background-color: #f1f3f5; padding: 20px; border-radius: 8px; margin-bottom: 25px; border-left: 5px solid #343a40; }
        .admin-title { display: block; margin-bottom: 15px; font-weight: bold; color: #343a40; }
        .admin-actions { display: flex; gap: 10px; flex-wrap: wrap; }
        
        /* Botões do Admin */
        .btn-admin {
            display: inline-block;
            text-decoration: none;
            padding: 10px 15px;
            background-color: #343a40;
            color: white;
            border-radius: 5px;
            font-weight: bold;
            font-size: 0.9em;
            border: none;
            transition: background-color 0.2s;
        }
        .btn-admin:hover { background-color: #23272b; color: white; }


        /* --- Filtros --- */
        .filtros-box {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            border: 1px solid #eee;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
            margin: 25px 0;
        }
        .filtros-header { display: block; margin-bottom: 15px; font-weight: bold; color: #555; }
        .filtros-box form { display: flex; gap: 15px; align-items: flex-end; flex-wrap: wrap; }
        .filtros-box label { display: flex; flex-direction: column; gap: 5px; font-size: 0.9em; color: #666; font-weight: bold; }
        .filtros-box input, .filtros-box select { padding: 8px; border: 1px solid #ccc; border-radius: 4px; }

        /* Botão Filtrar */
        .btn-filter { background-color: #007bff; color: white; border: none; padding: 9px 20px; border-radius: 4px; cursor: pointer; font-weight: bold; transition: background-color 0.2s; }
        .btn-filter:hover { background-color: #0056b3; }
        .link-limpar { text-decoration: none; color: #6c757d; font-size: 0.9em; padding: 9px 5px; }
        .link-limpar:hover { text-decoration: underline; color: #495057; }


        /* --- Tabela --- */
        table { width: 100%; border-collapse: separate; border-spacing: 0; margin-top: 20px; border: 1px solid #eee; border-radius: 8px; overflow: hidden; box-shadow: 0 2px 5px rgba(0,0,0,0.05); background: #fff; }
        th, td { padding: 12px 15px; text-align: center; border-bottom: 1px solid #eee; }
        th { background-color: #f8f9fa; color: #495057; font-weight: 600; }
        tr:last-child td { border-bottom: none; }
        
        /* Status Labels */
        .status-label { padding: 5px 10px; border-radius: 50px; font-weight: bold; font-size: 0.85em; text-transform: uppercase; }
        .status-agendado { background-color: #fff3cd; color: #856404; }
        .status-realizado { background-color: #d4edda; color: #155724; }
        .status-cancelado { background-color: #f8d7da; color: #721c24; }

        /* --- Botões de Ação na Tabela (Sem Emojis) --- */
        .btn-action-group { display: flex; gap: 5px; justify-content: center; align-items: center; }
        .btn-action {
            cursor: pointer;
            border: none;
            padding: 6px 10px;
            border-radius: 4px;
            color: white;
            font-weight: bold;
            font-size: 0.8em;
            transition: background-color 0.2s;
        }
        .btn-check { background-color: #28a745; }
        .btn-check:hover { background-color: #218838; }
        
        .btn-cancel { background-color: #dc3545; }
        .btn-cancel:hover { background-color: #c82333; }

        /* Botão de Editar (Estilo Específico) */
        .btn-edit { background-color: #007bff; text-decoration: none; display: inline-block; }
        .btn-edit:hover { background-color: #0056b3; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Agenda da Barbearia</h1>
        <div class="user-info">
            <span>Bem-vindo!</span>
            
            <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
                <span class="badge-admin">ADMIN</span>
            <% } %>
            
            <a href="/logout" class="btn-logout">Sair</a>
        </div>
    </div>

    <div class="nav-links">
        <a href="/" class="nav-btn">Home</a>
        <a href="/agendamentos/criar" class="nav-btn">Novo Agendamento</a>
        <a href="/profissionais" class="nav-btn">Profissionais</a>
        <a href="/servicos" class="nav-btn">Serviços</a>
    </div>

    <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
        <div class="admin-panel">
            <span class="admin-title">Painel do Administrador</span>
            <div class="admin-actions">
                <a href="/registrar" class="btn-admin">Cadastrar Novo Usuário</a>
                <a href="/profissionais" class="btn-admin">Gerenciar Profissionais</a>
                <a href="/servicos" class="btn-admin">Gerenciar Serviços</a>
            </div>
        </div>
    <% } %>
    
    <div class="filtros-box">
        <span class="filtros-header">Filtrar Agendamentos</span>
        <form action="/agendamentos" method="GET">
            <label>
                Data: <input type="date" name="data">
            </label>
            
            <label>
                Status:
                <select name="status">
                    <option value="">Todos</option>
                    <option value="agendado">Agendado</option>
                    <option value="realizado">Realizado</option>
                    <option value="cancelado">Cancelado</option>
                </select>
            </label>

            <div>
                 <span style="display: block; height: 22px;"></span>
                 <button type="submit" class="btn-filter">Buscar</button>
                 <a href="/agendamentos" class="link-limpar">Limpar filtros</a>
            </div>
        </form>
    </div>

    <table>
        <thead>
            <tr>
                <th>Data</th>
                <th>Hora</th>
                <th>Profissional</th>
                <th>Serviço</th>
                <th>Status</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            <% if (agendamentos.length === 0) { %>
                <tr>
                    <td colspan="6" style="color: #666; padding: 30px; font-style: italic;">
                        Nenhum agendamento encontrado para os filtros selecionados.
                    </td>
                </tr>
            <% } %>

            <% agendamentos.forEach(a => { %>
                <tr>
                    <td><%= new Date(a.dataHora).toLocaleDateString('pt-BR', {timeZone: 'America/Sao_Paulo'}) %></td>
                    <td><%= new Date(a.dataHora).toLocaleTimeString('pt-BR', {hour: '2-digit', minute:'2-digit', timeZone: 'America/Sao_Paulo'}) %></td>
                    
                    <td><%= a.Profissional ? a.Profissional.nome : 'N/A' %></td>
                    <td><%= a.Servico ? a.Servico.nome : 'N/A' %></td>
                    
                    <td>
                        <span class="status-label status-<%= a.status %>"><%= a.status %></span>
                    </td>
                    
                    <td>
                        <% if (a.status === 'agendado') { %>
                            <div class="btn-action-group">
                                <form action="/agendamentos/<%= a.id %>/concluir" method="POST" style="display:inline;">
                                    <button class="btn-action btn-check" title="Marcar como Realizado">Concluir</button>
                                </form>

                                <a href="/agendamentos/<%= a.id %>/editar" class="btn-action btn-edit" title="Editar Agendamento">Editar</a>

                                <form action="/agendamentos/<%= a.id %>/cancelar" method="POST" style="display:inline;">
                                    <button class="btn-action btn-cancel" title="Cancelar Agendamento">Cancelar</button>
                                </form>
                            </div>
                        <% } else { %>
                            <span style="color: #ccc;">-</span>
                        <% } %>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</body>
</html>